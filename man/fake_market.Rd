% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/synthetic_market_generation.R
\name{fake_market}
\alias{fake_market}
\alias{synthetic_market}
\title{Generate a reproducible synthetic market design}
\usage{
fake_market(
  mode = "observed",
  n_firms = 3L,
  n_products = 1L,
  dirichlet_alpha = rep(1, n_firms * n_products),
  outside_beta = c(2, 8),
  prices = NULL,
  reference_price = NULL,
  price_rule = c("common", "uniform"),
  price_level = 100,
  price_range = c(50, 150),
  outside_margin = NULL,
  observed_markup = NULL,
  reference_markup = NULL,
  markup_range = c(0, 100),
  parameters = list(),
  alpha = NULL,
  seed = NULL
)

synthetic_market(...)
}
\arguments{
\item{mode}{Either `"observed"` or `"primitives"`.}

\item{n_firms}{Number of inside firms; the reference firm is additional.}

\item{n_products}{Number of products owned by each inside firm. Defaults to
one.}

\item{dirichlet_alpha}{A positive finite vector of length
`n_firms * n_products`.}

\item{outside_beta}{A positive finite length-two vector of Beta shapes.}

\item{prices}{Optional positive prices for inside products or all products.}

\item{reference_price}{Price appended when only inside prices are supplied.}

\item{price_rule}{Either `"common"` or `"uniform"` when prices are not
supplied.}

\item{price_level}{Common positive price and default reference price.}

\item{price_range}{Two positive endpoints for the uniform price rule.}

\item{outside_margin}{Optional observed reference-product level margin in
observed mode. If omitted it is drawn from the open numerical
implementation of `U(0, 100)`.}

\item{observed_markup}{Backward-compatible alias for `outside_margin`.}

\item{reference_markup}{Backward-compatible alias for `outside_margin`.}

\item{markup_range}{Two endpoints for the observed markup draw.}

\item{parameters}{A list of model-specific known primitives in primitives
mode, such as `list(alpha = -1)`.}

\item{alpha}{Optional shorthand for `parameters$alpha`.}

\item{seed}{An optional explicit integer seed. The generated effective seed
is always recorded in the returned object.}

\item{...}{Arguments passed to [fake_market()].}
}
\value{
A `SyntheticMarket` object.
}
\description{
The generator has two conceptually distinct modes. `mode = "observed"`
draws shares, prices, and one observed reference-product level markup. It
does not infer a demand or conduct primitive. `mode = "primitives"` records
supplied portable primitives as truth for a downstream economic adapter.
}
\details{
`n_firms` counts inside firms. The active reference product is an additional
one-product firm and is included in the ownership matrix.

All inside firms have the same number of products, controlled by
`n_products`. The Dirichlet draw is at the product level, so the vector
`dirichlet_alpha` has one element for each inside product. Firm shares are
the sums of their product shares; there is no equal-within-firm allocation
step.
}
